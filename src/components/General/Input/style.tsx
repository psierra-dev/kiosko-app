import styled from "styled-components";

export const SInput = styled.div`
    &{
        gap: 0.2em;
        width: 100%;
    }
    & label {
        font-size: 0.8em;
        font-weight: bold;
        margin-bottom: 3px;
    }

    .message {
        margin: 0;
        padding: 0;
        color: red;
        font-size: 0.9em;
    }
`;
